# Copy to .env and fill in. eval.py reads it without extra dependencies.

OPENAI_API_KEY=<your-openai-api-key>
OPENAI_MODEL=gpt-5.6-luna

# Optional: cost reporting. Price per 1M tokens, in USD. Leave at 0 to show tokens only.
OPENAI_PRICE_INPUT_PER_1M=0.20
OPENAI_PRICE_OUTPUT_PER_1M=1.20
